# For details on buf.yaml configuration, visit https://buf.build/docs/configuration/v2/buf-yaml
version: v2
lint:
  use:
    - STANDARD
    - SERVICE_SUFFIX
  # except:
    # - RPC_REQUEST_RESPONSE_UNIQUE  # отключает проверку на уникальность типов

breaking:
  use:
    - FILE
    - WIRE_JSON
    - PACKAGE

modules:
  - path: proto

deps:
  - buf.build/protocolbuffers/wellknowntypes
  - buf.build/bufbuild/protovalidate
  

